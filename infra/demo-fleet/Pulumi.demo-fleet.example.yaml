# Example only. Do not commit real tenant IDs, subscription IDs, or resource IDs.
config:
  demoFleet:defaultLocation: eastus
  demoFleet:targets:
    - id: demo-target-01
      tenantId: <tenant-id>
      subscriptionId: <subscription-id>
      targetGroup: canary
      region: centralus
      environment: prod
      tier: gold
      customerName: Demo Customer A
  demoFleet:existingManagedEnvironmentIdsBySubscription: {}
